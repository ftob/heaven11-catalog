server-php:
  image: quay.io/optionlift/php:latest
  tty: true
  stdin_open: true
  volumes_from:
    - server
  external_links:
    - ${RABBIT_HOST}:rabbit
  environment:
    RABBIT_PORT: ${RABBIT_PORT}
    RABBIT_USER: ${RABBIT_USER}
    RABBIT_PASSWORD: ${RABBBIT_PASSWORD}
  labels:
    io.rancher.sidekicks: php, nginx
server-nginx:
  image: quay.io/optionlift/nginx
  tty: true
  stdin_open: true
  volumes_from:
    - server
  links:
    - server-php
  labels:
    io.rancher.sidekicks: server

server:
  image: quay.io/optionlift/server:latest
